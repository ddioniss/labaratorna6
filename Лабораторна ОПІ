#include <iomanip>
#include <iostream>
#include <math.h>
using namespace std;

int main()
{
int l;
double l;
    int a, k;
    double h;
    cout << "Введіть а: ";
    cin >> a;
    cout << "Введіть k: ";
    cin >> k;
    // Цикл while
    while (x <= k)
    {
        y = exp(x) + abs(x) - pow(x, 7);
        cout << setw(10) << "x = " << x << setw(10) << "y = " << y << endl;
        x += h;
    } 
    // Цикл do ... while
    do {
        y = exp(x) + abs(x) + pow(x, 5);
        cout << setw(10) << "x = " << x << setw(10) << "y = " << y << endl;
        x += h;
    } while (x <= k);
}
